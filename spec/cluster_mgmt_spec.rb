require 'spec_helper'

describe ClusterMgmt do
  it 'has a version number' do
    expect(ClusterMgmt::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
